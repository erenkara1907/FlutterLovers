import 'package:flutter/material.dart';

final ButtonStyle googleStyle = ElevatedButton.styleFrom(
  primary: Colors.white,
  minimumSize: const Size(30, 40),
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(Radius.circular(16),),
  ),
);

final ButtonStyle facebookStyle = ElevatedButton.styleFrom(
  primary: Colors.blue.shade900,
  minimumSize: const Size(30, 40),
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(Radius.circular(16),),
  ),
);

final ButtonStyle emailPasswordStyle = ElevatedButton.styleFrom(
  primary: Colors.purple,
  minimumSize: const Size(30, 40),
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(Radius.circular(16),),
  ),
);

final ButtonStyle anonymousStyle = ElevatedButton.styleFrom(
  primary: Colors.green.shade800,
  minimumSize: const Size(30, 40),
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(Radius.circular(16),),
  ),
);

final ButtonStyle loginStyle = ElevatedButton.styleFrom(
  primary: Colors.purple,
  minimumSize: const Size(30, 40),
  shape: const RoundedRectangleBorder(
    borderRadius: BorderRadius.all(Radius.circular(16),),
  ),
);

